# OCR_Internship
AI-powered document processing pipeline that classifies scanned business documents (Invoice, Contract, Report) using OCR, TF-IDF + Logistic Regression, and extracts structured fields (dates, amounts, org names, IDs) via NLP — served through a Flask REST API.
